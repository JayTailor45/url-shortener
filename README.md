# URL Shortener

## Node-backend
- express web server build on top of nodejs runtime

## Requirements
- docker with compose

## Running the project
- Run `docker compose up`
- To stop the project, run `docker compose down`